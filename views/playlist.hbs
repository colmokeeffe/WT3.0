{{> menu}}

<section class="ui center aligned middle aligned segment">
  <h2 class="ui header">
    {{playlist.title}}
  </h2>
  <h4> Shortest song: {{shortestSong.title}}</h4>
  {{> listsongs}}
  {{> addsong}}
</section>
